{
  "name": "emobox-sql",
  "version": "1.0.0",
  "private": true,
  "description": "EmoBox static site with a local SQL Server persistence API.",
  "scripts": {
    "local": "node local-server.js",
    "check": "node --check local-server.js && node --check api.js"
  }
}
