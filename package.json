{
  "name": "emobox-sql",
  "version": "1.0.0",
  "private": true,
  "description": "EmoBox static site with Vercel API routes backed by Neon Postgres.",
  "scripts": {
    "check": "node --check api.js && node --check lib/db.js && node --check lib/email.js && node --check api/index.js"
  },
  "dependencies": {
    "nodemailer": "^8.0.7",
    "pg": "^8.20.0"
  }
}
