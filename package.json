{
  "name": "emobox-sql",
  "version": "1.0.0",
  "private": true,
  "description": "EmoBox static site with a local SQL Server persistence API.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check api.js"
  }
}
